# ⚠️ WARNING: FOR DEMO/DEVELOPMENT ONLY! ⚠️
# This file is committed to the repository for demonstration purposes.
# In a real production environment:
# 1. Remove this file from git: git rm --cached frontend/.env
# 2. Add .env back to .gitignore
# 3. Use platform-specific environment variables (Vercel, Netlify, etc.)
# 4. Set VITE_API_URL to your production API endpoint

# API Configuration
VITE_API_URL=http://localhost:5000/api
